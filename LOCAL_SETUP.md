# تشغيل Enercyra محليًا

هذه النسخة تحتوي على كود الواجهة والباك إند وملفات نموذج MobileNetV3 وملفات نموذج LSTM والتدرّج الخاص به. لا تحتوي الحزمة على مفاتيح الدخول أو أسرار السحابة.

## المتطلبات

يُفضّل استخدام Node.js 22 أو أحدث، مع pnpm 10 أو أحدث. لتشغيل ميزة تصنيف الصور محليًا يلزم Python 3 و`onnxruntime`؛ أسهل طريقة هي استخدام Docker لأن ملف `Dockerfile` الموجود في المشروع يجهّز بيئة التصنيف تلقائيًا.

## التشغيل العادي للتطوير

من داخل مجلد المشروع شغّل:

```bash
pnpm install
pnpm dev
```

بعدها افتح:

```text
http://localhost:3000/
http://localhost:3000/forecast
```

صفحة `/forecast` تعمل على إرسال 14 قيمة kWh إلى `POST /api/forecast` وإرجاع توقعات الأيام السبعة التالية. ملفات `server/models/lstm_model.pt` و`server/models/scaler.pkl` موجودة داخل النسخة ويتم تحميلها مرة واحدة عند بدء الخادم. يستخدم الخادم ملف JSON المحسّن للاستدلال السريع مع الاحتفاظ بملفات النموذج الأصلية للمراجعة وإعادة التدريب.

## التشغيل باستخدام Docker

من داخل مجلد المشروع:

```bash
docker build -t enercyra-local .
docker run --rm -p 3000:3000 -e PORT=3000 enercyra-local
```

ثم افتح `http://localhost:3000`. إذا أردت وظائف الحسابات أو التخزين السحابي أو تسجيل الدخول، أضف متغيرات البيئة الخاصة بك عند تشغيل الحاوية، مثل `DATABASE_URL` و`JWT_SECRET` و`VITE_APP_ID` و`OAUTH_SERVER_URL` وبيانات Forge. لا تضع مفاتيح حقيقية داخل ملفات المشروع أو داخل الحزمة المضغوطة.

## اختبار سريع للـ API

بعد تشغيل الخادم، يمكن اختبار التنبؤ من الطرفية:

```bash
curl -X POST http://localhost:3000/api/forecast \\
  -H "content-type: application/json" \\
  -d '{"values":[150,152,148,160,155,162,158,165,170,168,172,169,175,171]}'
```

يجب أن يرجع الرد كائن JSON يحتوي على `predictions` وبداخله سبع قيم، إضافة إلى `lookback: 14` و`horizon: 7` و`unit: "kWh"`.

## الاختبارات والبناء

```bash
pnpm test
pnpm check
pnpm build
```

لإيقاف خادم التطوير استخدم `Ctrl+C`. في Windows يمكن تنفيذ الأوامر من PowerShell داخل مجلد المشروع، مع تثبيت Node.js وpnpm أولًا.

> ملاحظة: الموقع المنشور على السحابة يظل متاحًا بصورة مستقلة. النسخة المحلية هي نسخة تشغيل وتطوير على جهازك، وقد تحتاج إلى متغيرات بيئة خاصة بك لتفعيل الحسابات والخصائص السحابية.
