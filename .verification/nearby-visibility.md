# Nearby facility visibility verification

The Businesses page now shows a visible bilingual location-matching CTA in the heading: Arabic `اعثر على أقرب جهة مناسبة` / English `Find a nearby compatible facility`. The CTA anchors to the existing `#nearby-facilities` panel, which remains visible below the heading and retains the existing premium dark layout. The panel also receives the material query hint, e.g. `Plastic`, and keeps the explicit `Use my location` action.

Verification date: 2026-08-27.
